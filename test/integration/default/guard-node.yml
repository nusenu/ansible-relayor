---
- hosts: all
  vars_files:
    - vars/dry-run-vars.yml
  vars:
    tor_gen_ciiss_proof_files: True
    tor_cpu_affinity: True
  roles:
    - "{{ playbook_dir | regex_replace('test/integration/default$') }}"
